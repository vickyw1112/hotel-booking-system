/**
 * 
 */
package ass1;

import java.io.File;
import java.io.FileNotFoundException;
import java.io.IOException;
import java.util.LinkedList;
import java.util.Scanner;

/**
 * @author vicky
 *
 */
public class HotelBookingSystem {
	
	public boolean manageInput(String input) {
		String [] inputs = input.split(" ");
		switch (inputs[0]) {
		case "Hotel":
			Hotel hotel = new Hotel(inputs[1]);
			
		}
			
		
	}
	

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		Scanner sc = null;
	    String input;
		int i;
		HotelSystem hs = new HotelSystem();
	      try{
	          sc = new Scanner(new File(args[0]));    // args[0] is the first command line argument
	          // Read input from the scanner here
	          while(sc.hasNextLine()) {
	        	  input = sc.nextLine();
	        	  manageImput(HotelSystem hs, input);
	          }
	      }
	      catch (FileNotFoundException e){
	          System.out.println(e.getMessage());
	      }
	      finally{
	          if (sc != null) sc.close();
	      }
		
	}

}
